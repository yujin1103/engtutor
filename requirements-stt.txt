# 음성 인식 측정용. 아직 앱에 넣지 않았으므로 기본 이미지에는 굽지 않는다.
#
# 왜 따로 두는가
# --------------
# STT 를 채택할지가 아직 측정 결과에 달려 있다. Whisper 가 학습자의 틀린 영어를
# 매끄럽게 고쳐 적으면 교정 채널이 무의미해지므로, 그 비율을 먼저 재고 정한다
# (scripts/eval_stt.py). 채택하기로 하면 requirements.txt 로 옮긴다.
#
# 설치:
#   docker compose exec api pip install -r requirements-stt.txt
faster-whisper>=1.0
