# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27
PyYAML>=6.0
SQLAlchemy>=2.0

# --- LLM 백엔드 ---
anthropic>=0.69

# --- UI ---
streamlit>=1.40

# --- 테스트 ---
pytest>=8.3

# --- 어휘 사실 확인 (환각 검사) ---
# WordNet 조회로 생성된 콘텐츠의 품사 단정을 반증한다. 코퍼스는 Dockerfile 에서
# 이미지에 굽는다 — 런타임에 내려받으면 오프라인에서 검사가 조용히 꺼진다.
nltk>=3.9
